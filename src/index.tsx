import React from 'react';
import ReactDOM from 'react-dom';
import Orchestrator from "./page/Orchestrator/Orchestrator";

ReactDOM.render(
  <React.StrictMode>
    <Orchestrator />
  </React.StrictMode>,
  document.getElementById('root')
);

